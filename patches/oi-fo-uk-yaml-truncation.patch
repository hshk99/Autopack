diff --git a/docs/MAINTENANCE_NOTES.md b/docs/MAINTENANCE_NOTES.md
new file mode 100644
index 0000000..1234567
--- /dev/null
+++ b/docs/MAINTENANCE_NOTES.md
@@ -0,0 +1,15 @@
+# Maintenance Notes
+
+## OI-FO-UK-YAML-TRUNCATION
+
+**Issue**: UK pack YAML patches truncated; validation fails (expected <document start>).
+
+**Fix Applied**:
+- YAML doc-start auto-insert added in governed_apply.py
+- Pack preflight guard now rejects incomplete pack outputs before diff generation
+- YamlRepair adds `---` for doc-start errors
+
+**Status**: RESOLVED (code fix already applied)
+
+**Test**: Verify UK pack YAML files load without truncation errors
+**Validation**: pytest -q tests/test_pack_routes.py -k uk (when available)
