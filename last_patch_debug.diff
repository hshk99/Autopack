diff --git a/src/backend/api/runs.py b/src/backend/api/runs.py
index 1234567..abcdefg 100644
--- a/src/backend/api/runs.py
+++ b/src/backend/api/runs.py
@@ -1,10 +1,11 @@
 """Runs API endpoints"""
 
-from fastapi import APIRouter, Depends, HTTPException
+from fastapi import APIRouter, Depends, HTTPException, status
 from sqlalchemy.orm import Session
-from pydantic import BaseModel
+from pydantic import BaseModel
+from typing import Optional
 
-from ..database import get_db
+from ..database import get_db
 from ..models import Run
 
 router = APIRouter(prefix="/runs", tags=["runs"])
@@ -1,6 +1,14 @@
     status: str | None = None
     goal_anchor: str | None = None
 
+
+class CreateRunRequest(BaseModel):
+    id: str  # run_id
+    goal_anchor: Optional[str] = None
+    safety_profile: str = "normal"
+    run_scope: str = "multi_tier"
+
+
 @router.get("/{run_id}")
 def get_run(run_id: str, db: Session = Depends(get_db)):
     """Get a run by ID"""
@@ -1,6 +1,28 @@
         raise HTTPException(status_code=404, detail="Run not found")
     return run
 
+
+@router.post("", status_code=status.HTTP_201_CREATED)
+def create_run(request: CreateRunRequest, db: Session = Depends(get_db)):
+    """Create a new run"""
+    # Check if run already exists
+    existing_run = db.query(Run).filter(Run.id == request.id).first()
+    if existing_run:
+        raise HTTPException(status_code=409, detail="Run already exists")
+    
+    # Create new run
+    run = Run(
+        id=request.id,
+        goal_anchor=request.goal_anchor,
+        safety_profile=request.safety_profile,
+        run_scope=request.run_scope,
+        status="pending"
+    )
+    db.add(run)
+    db.commit()
+    db.refresh(run)
+    return run
+
 
 @router.patch("/{run_id}")
 def update_run(run_id: str, request: UpdateRunRequest, db: Session = Depends(get_db)):
