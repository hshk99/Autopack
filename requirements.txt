# Core FastAPI dependencies
fastapi>=0.104.0,<0.105.0
starlette>=0.27.0,<0.51.0  # pin to FastAPI-compatible range to avoid routing regressions
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-multipart>=0.0.6

# Database
sqlalchemy>=2.0.23
psycopg2-binary>=2.9.9
alembic>=1.13.0

# Testing
pytest>=7.4.3
pytest-asyncio>=0.21.1
pytest-cov>=4.1.0
httpx>=0.25.2,<0.26.0  # pin to avoid breaking changes in 0.26+

# Task queue and file validation
python-magic>=0.4.27; sys_platform != 'win32'
python-magic-bin>=0.4.14; sys_platform == 'win32'

# JWT
python-jose[cryptography]>=3.3.0

# Vector store (Qdrant is default; FAISS optional for dev/offline)
qdrant-client>=1.7.0