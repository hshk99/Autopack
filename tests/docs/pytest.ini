[pytest]
# Documentation tests are for verifying documentation integrity,
# not for code coverage. Disable coverage measurement to avoid
# skewing overall project coverage metrics.
addopts =
    --verbose
    --strict-markers
    --tb=short
    --no-cov
